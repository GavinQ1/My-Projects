Program theUltimateTest (input,output);
Var
   i,x : real;

procedure this (why, note : real);
begin
   if ((why = note - 1608) or not (note = why))
   then if (x - i = 0)
      then begin
         x := why / note
         end
end

begin
   i := 1;
   x := 5;
   this (i,x);
   write(i,x)
end.

